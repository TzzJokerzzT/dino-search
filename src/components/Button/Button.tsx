import { Button } from "@nextui-org/button";

const Buttons = () => {
  return (
    <Button
      className="text-black dark:text-white"
      color="primary"
      type="submit"
      variant="bordered"
    >
      Search
    </Button>
  );
};

export default Buttons;
